# Copie este arquivo para ".env" (mesma pasta do docker-compose.yml) e
# preencha os valores reais antes de rodar "docker compose up".
DB_USERNAME=postgres
DB_PASSWORD=troque-esta-senha
DB_PORT=5433
JWT_SECRET=troque-este-segredo-em-producao-min-32-caracteres
OPENAI_API_KEY=sk-...
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Recuperação de senha por email (opcional). Sem MAIL_HOST, o backend só loga o
# link no console em vez de enviar — o fluxo continua funcionando pra testes.
FRONTEND_BASE_URL=http://localhost:5173
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=no-reply@entrevista-ia.local
